<Window x:Class="GUI.ExitMsgBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Height="180" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Name="Label" 
                   HorizontalAlignment="Stretch" 
                   VerticalAlignment="Top"
                   TextWrapping="Wrap"
                   Margin="6,0,6,10"/>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Name="Button1" Grid.Row="0" Grid.Column="0" 
                    Height="25" Margin="5"/>

            <Button Name="Button2" Grid.Row="1" Grid.Column="0" 
                    Height="25" Margin="5"/>

            <Button Name="Button4" Grid.Row="1" Grid.Column="1" 
                    Height="25" Margin="5"/>

            <Button Name="Button3" Grid.Row="0" Grid.Column="1"
                    Height="25" Margin="5"/>
        </Grid>
    </Grid>
</Window>
